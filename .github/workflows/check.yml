name: check

on:
  pull_request:
  push:
    branches:
      - main
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: "lts/*"
          cache: "npm"

      - run: npm ci
      - run: npm run tsc
      - run: npm run test
      - run: npm run build
      - run: npm run size
      - run: npm run format:ci

  e2e:
    timeout-minutes: 15
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: "lts/*"
          cache: "npm"

      - run: npm ci
      - name: Install Playwright Browsers
        run: npx playwright install --with-deps
      - run: npm run test:browser
      - name: Run Playwright tests
        run: npm run e2e

      - run: npm run build

      - name: Smoke react
        run: (cd examples/react && npm ci && npm install --no-save edix@file:../..) && FRAMEWORK_DIR=examples/react npm run e2e
      - name: Smoke vue
        run: (cd examples/vue && npm ci && npm install --no-save edix@file:../..) && FRAMEWORK_DIR=examples/vue npm run e2e
      - name: Smoke svelte
        run: (cd examples/svelte && npm ci && npm install --no-save edix@file:../..) && FRAMEWORK_DIR=examples/svelte npm run e2e
      - name: Smoke solid
        run: (cd examples/solid && npm ci && npm install --no-save edix@file:../..) && FRAMEWORK_DIR=examples/solid npm run e2e
      - name: Smoke angular
        run: (cd examples/angular && npm ci && npm install --no-save edix@file:../..) && FRAMEWORK_DIR=examples/angular npm run e2e
      - name: Smoke preact
        run: (cd examples/preact && npm ci && npm install --no-save edix@file:../..) && FRAMEWORK_DIR=examples/preact npm run e2e
      - name: Smoke vanilla
        run: (cd examples/vanilla && npm ci && npm install --no-save edix@file:../..) && FRAMEWORK_DIR=examples/vanilla npm run e2e
      # - uses: actions/upload-artifact@v3
      #   if: always()
      #   with:
      #     name: playwright-report
      #     path: playwright-report/
      #     retention-days: 30
